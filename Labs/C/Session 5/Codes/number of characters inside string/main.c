#include <stdio.h>
int main()
{
    int counter=0;
   char name[20]="3alamny";
   while(name[counter]!='\0')
   {
       counter++;0
   }
   printf("the number of characters inside string=%i",counter);
    return 0;
}
